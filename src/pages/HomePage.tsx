import Hero from "../components/Hero";

const HomePage = () => {
  return (
    <main className="bg-hero-image h-screen w-screen bg-cover bg-center bg-no-repeat p-[2rem] ">
      <Hero />
    </main>
  );
};

export default HomePage;
